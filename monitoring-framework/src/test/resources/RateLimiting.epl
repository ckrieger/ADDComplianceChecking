@Name('Rate Limiting Violation') insert into RateLimitViolation select count(*) from HttpRequestEvent().win:time_batch(${interval} sec) where statusCode = '200' group by serviceId having count(*) > ${requestLimit};
